# sign_up_form
Simple sign up form that has a state selection drop down as well as the counties corresponding to the selected state. long/lat of the area code input is displayed. There is also a username checker as well as a password checker. If any field fails the sign up will fail until those fields are corrected.
